#pragma once

#include <cstdint>

namespace log_slayer {

    namespace options {

        constexpr auto SHARED_MEMORY_NAME = "@shm_name@";

        constexpr bool SHARED_MEMORY_DYNAMIC = @shm_dynamic@;

        constexpr std::size_t SHARED_MEMORY_MAX_THREADS = @shm_max_threads@;

        constexpr std::size_t EVENT_ALIGN = @event_align@;

        constexpr std::size_t EVENTS_PER_THREAD = @events_per_thread@;

        constexpr bool ENABLE_EVENT_TYPE = @enable_event_type@;

        constexpr bool ENABLE_EVENT_TIMESTAMP = @enable_event_timestamp@;

        constexpr auto SAVE_PATH = "@save_path@";

    }

}

